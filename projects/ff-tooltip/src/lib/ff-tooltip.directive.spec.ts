import { FfTooltipDirective } from './ff-tooltip.directive';

describe('FfTooltipDirective', () => {
  it('should create an instance', () => {
    const directive = new FfTooltipDirective();
    expect(directive).toBeTruthy();
  });
});
